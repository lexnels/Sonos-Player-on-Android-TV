# Releases

Installable APKs for the current version. Gradle copies each packaged build here automatically (`assembleDebug`, `assembleRelease`, `installDebug`, and so on).

Filenames follow `SonosTV-<versionName>(<versionCode>)-<variant>.apk`. A new build of the same variant replaces the previous APK for that variant.
